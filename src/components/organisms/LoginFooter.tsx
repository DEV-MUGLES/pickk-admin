import {Layout} from 'antd'

const {Footer} = Layout;

export default function LoginFooter() {
    return(
        <Footer style={{ textAlign: "center" }}>
            로그인푸터
        </Footer>
    );  
    
}